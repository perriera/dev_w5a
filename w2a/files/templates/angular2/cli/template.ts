import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-((includeText))',
  templateUrl: './((includeText)).component.html',
  styleUrls: ['./((includeText)).component.css']
})
export class ((identifierText)) implements OnInit {

  constructor() { }

  ngOnInit() {
  }

}

/* tslint:disable:no-unused-variable */
import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { By } from '@angular/platform-browser';
import { DebugElement } from '@angular/core';

import { ((identifierText)) } from './((includeText))';

describe('((identifierText))', () => {
  let component: ((identifierText));
  let fixture: ComponentFixture<((identifierText))>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ((identifierText)) ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(((identifierText)));
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
